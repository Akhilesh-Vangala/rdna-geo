# Context and requirements

## Goal

Professor’s ask: determine whether we can specifically obtain the sequences mapping to the rDNA for SRP126734. Deliverable: download SRA runs, align to rDNA reference only, produce rDNA BAMs and report read counts.

## Dataset

- SRP126734 = GSE108065. 54 WGS runs (29 schizophrenia, 25 controls). Illumina HiSeq X Ten, paired-end. Public on NCBI SRA.
- Run list: `data/run_list.txt`. Sample and phenotype mapping: `data/sample_metadata.tsv`.

## Data access

Raw data lives on NCBI SRA. Connection: run IDs in `data/run_list.txt` plus SRA Toolkit (`fasterq-dump`). No API key; public access. Scripts use these IDs to fetch FASTQ and then align to the rDNA reference.

## Requirements and status

- Dataset and run list: done. Phenotypes filled from GSE108065.
- Pipeline scripts (SRA → rDNA BAM): done; **example runs completed** (see `docs/run_records_three_samples.md`).
- rDNA reference FASTA: obtained from collaborators (Alka) via shared files.
- **Variant calling:** `scripts/variant_calling.sh` runs **bcftools** (`mpileup` → `call`) by default, producing per-sample `vcfs/<SRR>_rdna.vcf.gz`. Optional **`VARIANT_CALLER=gatk`** uses GATK HaplotypeCaller. Lab-specific **mgatk** pipelines are not bundled here (different tool/install).

## Collaborator scripts

Professor asked Alka and Kinjal to share the collaborators’ rDNA extraction/variant-calling workflow so this repo can match the lab. **Extraction** is implemented locally (`download_and_extract_rdna.sh`). **Generic variant calling** is in `variant_calling.sh` (bcftools/GATK). If the lab uses **mgatk** or custom filters end-to-end, replace or extend step 2 once they share exact commands — example **mgatk-style** output is in `geo scripts/HQ_mgatk_variants.tsv` (not generated by the default shell script).
